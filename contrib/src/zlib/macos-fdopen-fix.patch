--- a/zutil.h
+++ b/zutil.h
@@ -125,9 +125,8 @@
 #    if defined(__MWERKS__) && __dest_os != __be_os && __dest_os != __win32_os
 #      include <unix.h> /* for fdopen */
 #    else
-#      ifndef fdopen
-#        define fdopen(fd,mode) NULL /* No fdopen() */
-#      endif
+      /* macOS provides fdopen() in stdio.h, so no macro definition needed */
 #    endif
 #  endif
 #endif

